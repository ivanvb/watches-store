{% comment %}
    Displays a card with a brief text at the bottom.

    Accepts:
    - url: {String} Url to redirect when the card is clicked
    - title: {String} Card title
    - subtitle: {String} Card subtitle
    - image: {Image Object} Card Image
    - backgroundClass: {String} class to display a background color
    - containerClass: {String} class to be applied to the card container
{% endcomment%}

<a href="{{ url}}" class="group {{ containerClass }}">
    <div class="overflow-hidden">
        <img 
            src="{{ image.src | img_url: '500x500' }}"
            alt="{{ product.title }}"
            class="group-hover:scale-105 group-focus-visible:scale-105 transition-transform duration-500 {{ backgroundClass }}"
            height="500"
            width="500"
            loading="lazy"
        />
    </div>
    <div class="mt-4">
        <h3 class="font-bold">{{ title }}</h3>
        <span>{{ subtitle }}</span>
    </div>
</a>